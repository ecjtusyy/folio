# 安全与可达性说明
- 单管理员登录（.env 提供），HttpOnly Cookie session
- 私有文件：/api/files/{id}/download 需登录；OnlyOffice/PDF.js 可使用 token 或 session
- OnlyOffice 文档 URL 使用 server 容器内地址：http://server:8000/api/onlyoffice/doc/{id}?token=...
- TeX 编译仅在 APP_TMP_DIR 解压并超时控制

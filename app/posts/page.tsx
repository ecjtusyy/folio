import Link from 'next/link';
import { getAllPostsMeta } from '@/lib/posts';
export default function PostsPage(){const posts=getAllPostsMeta();return <section className="page-shell"><p className="eyebrow">Posts</p><h1 className="page-title">Articles</h1><p className="page-lead">Static posts loaded from local markdown files in <code>content/posts</code>.</p><div className="post-list">{posts.map((post)=><article key={post.slug} className="post-card panel"><h2><Link href={`/posts/${post.slug}`}>{post.title}</Link></h2><div className="post-meta">{post.date}</div><p className="post-summary">{post.summary}</p><Link href={`/posts/${post.slug}`} className="text-link">Read post →</Link></article>)}</div></section>}
